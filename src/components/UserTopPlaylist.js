// Legacy dashboard route is retired. Tunevera's supported UI is src/pages/Home.jsx,
// which routes YouTube and conversion requests through Netlify Functions.
export default function UserTopPlaylist() {
  return null;
}
